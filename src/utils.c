#include "../include/utils.h"
#include <ctype.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

Token *tokenise(char *str, int type) {
    int capacity = 10;
    Token *token = malloc(capacity * sizeof(Token));

    if (type == 1) {
    }

    return token;
}
